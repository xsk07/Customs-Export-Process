j$(document).ready(function () {
    j$(':input:enabled:visible:first').focus();
});

function BASetFocus(element) {
    // this function overrides the previous one un script.js to enable the focus on the first input, don't delete it
}